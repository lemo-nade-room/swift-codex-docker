services:
  dev:
    image: "lemonaderoom/swift-codex:latest"
    build:
      context: .
    tty: true
    volumes:
      - "home:/lemo"
      - ".:/lemo/workspace"
      - "build:/lemo/workspace/.build"
      - "${HOME}/.codex:/lemo/.codex"
      - "${HOME}/.gitconfig:/lemo/.gitconfig"
      - "/var/run/docker.sock:/var/run/docker.sock"
volumes:
  home:
  build:
